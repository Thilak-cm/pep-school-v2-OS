interface:
  display_name: "Implement Issue"
  short_description: "Implement an approved Pep OS plan, push a PR to master, and monitor CI"
  default_prompt: "Run the implement-issue workflow: load the approved .context/issue-plans plan, implement with TDD, run local verification, commit, push the branch, open a PR against master, monitor CI, and if CI fails launch ci-diagnostician followed by code-fixer until checks pass or the repair loop escalates."
